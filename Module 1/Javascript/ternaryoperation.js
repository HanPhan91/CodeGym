let dtb=-2;
console.log( (dtb<0 || dtb>10) ? ("Diem ko hop le") : (dtb>=9) ? 
                                                        ("Xuat sac") : (dtb>=8) ? 
                                                        ("Gioi") : (dtb>=7) ? 
                                                        ("Kha") : (dtb>=6) ? 
                                                        ("TB- Kha") : (dtb>=5) ? 
                                                        ("TB") : (dtb<5) ? 
                                                        ("Yeu") : ("") );